# Activity Log: REST API

**Date:** 2025-02-13  
**Participants:** @sniperman

## Summary

Implemented REST API endpoints for order management.

## Migration: Flask → FastAPI

Decided to migrate from Flask to FastAPI for:
- Native async support (WebSocket prep)
- Automatic OpenAPI docs
- Pydantic validation

## Endpoints Implemented

| Endpoint | Status | Notes |
|----------|--------|-------|
| `POST /orders` | ✅ | Returns order ID immediately, async match |
| `DELETE /orders/{id}` | ✅ | Cancel resting order |
| `GET /book/depth` | ✅ | L2 depth (price, volume) |
| `GET /trades` | ✅ | Recent trades with pagination |

## Authentication

API key middleware implemented:
```python
async def verify_api_key(x_api_key: str = Header(...)):
    if not validate_key(x_api_key):
        raise HTTPException(401, "Invalid API key")
    return x_api_key
```

## Load Testing

Simulated 1000 concurrent clients placing orders:
- p50 latency: 2ms
- p99 latency: 8ms
- No errors, no race conditions

## Next Steps

- WebSocket feeds
- Integration with main API for settlement
